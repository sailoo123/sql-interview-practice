SQL INTERVIEW PRACTICE LAB — SOURCE-CODE ADAPTATION

The supplied code snippets were used only for the useful application patterns:
- .hide CSS utility
- responsive @media behavior
- online/offline event handling pattern
- connection status UI

The Google Publisher Tag/ad calls and the external stpd.cloud script were NOT added because they are advertising/third-party code and are unrelated to the SQL practice functionality.

The adapted handlers now report whether the local SQL practice server is reachable.

This remains the v10 tutor edition base. Start with START.bat after extracting.
